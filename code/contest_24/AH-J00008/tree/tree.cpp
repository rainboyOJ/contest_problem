#include <bits/stdc++.h>
using namespace std;
int main(){
    int l, m;
    int a[10000] = {0};
    int l, m;
    int i, j;
    for(i = 1; i <= m; i++){
        int s, t;
        for(j = s; j <= t; j++) {
            a[j] = 1
        }
    }
    int cnt = 0;
    for(i = 0; i <= 1)
    if(!a[i]) cnt++;
    cout << cnt;
        return 0;
    }
