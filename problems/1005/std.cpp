#include<bits/stdc++.h>
using namespace std;
int main()
{
	string s,s1,s2;
	int i,p=0,len1,len2;
	getline(cin,s); 
	cin>>s1>>s2;
	
	
	
	
	len1=s1.size();
	len2=s2.size();
	while(1)
	{	p=s.find(s1,p);
		if(p==s.npos) break;
		
		
		
	}
	s.erase(0,1);
    
    
    
	cout<<s<<endl;
    return 0;
}


