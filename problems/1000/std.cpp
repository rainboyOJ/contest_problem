#include<bits/stdc++.h>
using namespace std;

const int MAXN=100;
long a[MAXN+5],n,m,sum=0;

int main()
{
	freopen("gwdml.in","r",stdin);
	freopen("gwdml.out","w",stdout); 
 	cin>>m>>n;
 	a[1]=1;
 	if (m>n) swap(m,n);
 	for (int i=1;i<m;i++)
 	
 	

 	for (int i=m;i<=n;i++)	
 	{
 		
 		
 		
 	}
 	cout<<sum<<endl;	
	return 0;
}

